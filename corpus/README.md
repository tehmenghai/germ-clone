# corpus/

Raw source documents for the ML Tutor RAG pipeline.

Drop PDFs, Markdown files, or plain-text documents here. This folder feeds the
ingestion pipeline (`source/backend/ingestion/`). No code lives here — content only.

Gitignored by default for large binaries. Add small sample/test documents to version
control explicitly if needed for CI.
